{
  "stable": {
    "96": [
      {
        "crate": {
          "path": "assert_matches!",
          "url": "https://docs.rs/assert_matches/latest/assert_matches/macro.assert_matches.html"
        },
        "note": "The crate's `=> arm` form, which yields a value or runs extra assertions on the bindings, has no `std` equivalent.",
        "std": {
          "path": "std::assert_matches!",
          "url": "https://doc.rust-lang.org/std/macro.assert_matches.html"
        }
      },
      {
        "crate": {
          "path": "debug_assert_matches!",
          "url": "https://docs.rs/assert_matches/latest/assert_matches/macro.debug_assert_matches.html"
        },
        "note": "The crate's `=> arm` form, which yields a value or runs extra assertions on the bindings, has no `std` equivalent.",
        "std": {
          "path": "std::debug_assert_matches!",
          "url": "https://doc.rust-lang.org/std/macro.debug_assert_matches.html"
        }
      }
    ]
  }
}